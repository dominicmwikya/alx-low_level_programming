#include "main.h"
#include <stdio.h>
/**
 * add -adds two integers and returns the result.
 * @x: to be added be y
 * @y: to be added to x
 * Return: sum of x and y
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
